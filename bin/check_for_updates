#!/bin/bash

if [[ $(cd ${DOT_DIR} && diff <(git rev-parse origin/master) <(git rev-parse HEAD) &>/dev/null; echo $?) -ne 0 ]]; then
  touch /tmp/.UPDATE_DOTFILES
else
  rm /tmp/.UPDATE_DOTFILES &>/dev/null
fi
